# تسليم جلسة 2026-07-05/06 — نظام الرسم والتخطيط الكامل

> جلسة ضخمة (~23 رفعة، `e4d4cff` → `10093bb`): أُعيد بناء الرسم والتحرير من
> الصفر ثم طُوّر تفاعليّاً مع المؤسّس حتى اكتمل. **المكتب مرسوم بالكامل**
> والمؤسّس ينظّم التخزين الآن.

## ✅ الترقيات المطبَّقة على القاعدة (تحقّقنا منها واحدة واحدة)

| ترقية | ماذا تضيف |
|---|---|
| 20 | `zones.points` JSONB — أشكال مضلّعة وجدران (open:true على أوّل نقطة، label:0/1 لإظهار الاسم) |
| 21 | `shelves.pos` JSONB `{top,left,width,height,kind,points?}` + `update_shelf_pos` — التقسيم الحرّ الداخلي |
| 22 | `zones.parent_zone_id` + `add_zone(zone_parent)` — مساحات داخل مساحات (تداخل بلا حدود) |

`get_warehouse_layout` يعيد الآن: `points`, `parent_zone_id`, `shelves[].pos`.

## الملفات الجديدة (`src/`)

- `lib/mapDraw.js` — هندسة خالصة: naturalZoneRect, pctFromClient, geometryFromAbsPoints,
  absPointsOfZone, orthoSnap, snapToTargets/Segments, straightenPoints (تقويم 45°), nearPoint, midPoint
- `components/MapDrawLayer.jsx` — طبقة الرسم (rect/poly/wall): معاينة حيّة، قياسات بالمتر على
  كل ضلع، إغلاق على النقطة الأولى، أبعاد مكتوبة (نقرة تضع مستطيلاً بمقاسه)، Esc/Enter/Backspace
- `components/VertexEditLayer.jsx` — محرّر نقاط ⬡: سحب/إضافة/حذف نقاط، ∟ تعامد، 📐 قوّم الشكل، حفظ صريح
- `components/ZoneInteriorEditor.jsx` — 🧰 تقسيم المكان: أنواع (➖رف/🗄درج/🗃خزانة/🎨شكلية/فاصل) + ↶ تراجع
- `components/NestedZonesPanel.jsx` — 🚪 مساحات داخل مساحة (📦 تخزين أو 🎨 شكلية) + ↶ تراجع، مطويّة إذا فارغة
- `components/MidMarks.jsx` + `CenterGuides.jsx` — علامات المنتصف وخطّا التنصيف (وضع التعديل)

## المفاهيم المفتاحية

- **اللصق أثناء السحب** (دائماً مفعّل): `stickEdge/stickAxis` في `gridConfig.js` داخل `useDragResize`
  — حواف + منتصفات + مراكز؛ **الجدار المفتوح يُلصق على نقاط خطّه الحقيقي** لا صندوق إحاطته.
- الرسم على الخريطة يُنشئ **عنصراً هيكليّاً فوراً** (بلا مودال)؛ زرّ **📦** يحوّله لتخزين بنفس شكله
  (ينشئ مساحة جديدة + يحذف العنصر — الحذف مفحوص). أزرار العنصر: ✏️🗑⬡👁📦 **ظاهرة دائماً في التعديل**.
- حروف العناصر تولَّد تلقائيّاً A..Z ثم W2..W99 (زوايا `zones.letter` TEXT + UNIQUE).
- المساحات تُنشأ **فارغة** (`rpcAddZone` fallback = 0 أرفف — «خلّها في يدي»).
- بطاقة المساحة على الخريطة = **نسخة مصغّرة حقيقية من الداخل** (أقسام pos + مساحات داخلية بمواضعها).
- `LocationPicker`: أبعاد حقيقية + قصّ مضلّعات + بلا عناصر هيكلية + المساحات الداخلية أزرار.
- الأقسام الحرّة خارج التعديل = **بطاقات مغلقة** (اسم+نوع+عدد) والضغط يدخل `ShelfView`.
- مكان له مساحات داخلية و0 أرفف → **واجهة الأرفف تُخفى** (كانت تبدو «مساحة ثانية» — التبس على المؤسّس مرّتين!).
- 🎨/⬜ زرّ خلفيات الخريطة (ملوّن/سادة) — localStorage `wm_plain_bg`.

## ⚠️ دروس لا تُنسى

- **`allZones` scope bug** (`d159df6`): متغيّر من المكوّن الأب استُخدم داخل WarehouseMapCanvas
  فكسر دخول كل المستودعات. الكانفس يرى `zones` (الجذرية فقط) و`data` — لا `allZones`.
- لوحتان متشابهتان فوق بعض = المستخدم يظنّها بيانات مكرّرة. قلّل الصناديق الظاهرة.
- المستخدم لا يعمل تحديثاً قويّاً تلقائيّاً — بعد كل نشر أخبره بعلامة مميّزة للنسخة الجديدة.

## ✅ أُنجزت لاحقاً في نفس الجلسة (كانت «قائمة قادمة»)

1. ✅ **الصور فوق الأشكال** — ترقية 23 **مُطبَّقة ومُتحقَّق منها**؛ زرّ 📷 على المساحات
   (خريطة) والأقسام (🧰)، `ShapePhotoModal` مشترك، الصورة خلفيّة مقصوصة بالشكل في كل العروض.
2. ✅ **معاينة WarehousesHome** — نسخة حقيقية: أبعاد فعليّة، مضلّعات مقصوصة، جدران خطوط،
   صور، شكليّة رماديّة، استبعاد children، **والأغراض الحرّة ظاهرة بصورها**.
3. ✅ **ملاءمة الشاشة** — `maxWidth: min(100%, calc(vh × نسبة الأبعاد))` على: الخريطة،
   واجهة الأرفف، محرّر 🧰، لوحة 🚪 — كل شيء ظاهر على أي جهاز بلا تمرير.
4. ✅ **الوضع الليلي** — LocationPicker كاملاً (sed جماعي dark:) + حقول BuilderForms.
5. ✅ **تسمية حرّة** — حرف أو اسم (أحدهما يكفي)؛ الحرف يولَّد تلقائيّاً؛ العرض يُظهر ما اختير فقط.
6. ✅ **تبديل النوع بالاتجاهين** 🎨↔📦 (خريطة + 🚪) بحماية من فقدان البيانات.
7. ✅ **حفظ/إلغاء في كل المحرّرات** — «↩ إلغاء التعديلات» يفكّ مكدس التراجع كاملاً.

## المتبقّي

- **الجوال 📱**: بانتظار أسوأ 3 ملاحظات من المؤسّس بعد تجربة حقيقية.
- تنظيف بقايا التجارب (عناصر/مساحات قديمة) — بيد المؤسّس عبر 🗑.
- رسم المكتب من صورة SketchUp: **أُلغي** — رسمه المؤسّس بيده بالأدوات الجديدة.
