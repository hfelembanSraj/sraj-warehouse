-- ============================================
-- ترقية رقم 14: تصنيفات/وسوم للأغراض
-- ============================================
-- يضيف عمود tags (مصفوفة نصوص) لكلّ غرض، وفهرس GIN للبحث السريع.
-- أمثلة: ['رياضي', 'فعاليّات'] أو ['إلكتروني', 'هام'] إلخ.
-- ============================================

ALTER TABLE items ADD COLUMN IF NOT EXISTS tags TEXT[] DEFAULT '{}';

-- فهرس GIN للبحث السريع في الوسوم
CREATE INDEX IF NOT EXISTS items_tags_gin_idx ON items USING gin(tags);

SELECT '✅ ترقية 14 (وسوم الأغراض) جاهزة' AS status;
